{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/server.cjs"
    }
  ],
  "builds": [
    {
      "src": "api/server.cjs",
      "use": "@vercel/node"
    }
  ],
  "outputDirectory": "dist",
  "installCommand": "npm install && cd api && npm install"
}